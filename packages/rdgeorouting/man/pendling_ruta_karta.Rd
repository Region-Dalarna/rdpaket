% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pendling.R
\name{pendling_ruta_karta}
\alias{pendling_ruta_karta}
\title{Karta (sf-objekt / GeoPackage) av resultatet från pendling_ruta()}
\usage{
pendling_ruta_karta(
  resultat,
  typ = c("in", "ut"),
  farg_fokusomrade = "black",
  skriv_till_gpkg = FALSE,
  ut_mapp = NA,
  gpkg_namn = NA
)
}
\arguments{
\item{resultat}{Listan som returneras av \code{\link[=pendling_ruta]{pendling_ruta()}}.}

\item{typ}{\code{"in"} eller \code{"ut"} - vilken pendling som ska visas
tillsammans med fokusområdet.}

\item{farg_fokusomrade}{Färg för fokusområdets rutor (kolumnen \code{farg},
\code{NA} för pendlingsrutorna - dessa styrs lämpligare via kolumnen
\code{antal_pendlare} med en färgskala när man kartlägger resultatet).}

\item{skriv_till_gpkg}{Skriv resultatet till en GeoPackage-fil.}

\item{ut_mapp}{Målmapp för GeoPackage.}

\item{gpkg_namn}{Filnamn för GeoPackage (\code{NA} = byggs automatiskt utifrån \code{typ}).}
}
\value{
Ett \code{sf}-objekt med kolumnerna \code{typ} (\code{"fokusomrade"},
\code{"in_pendling"} eller \code{"ut_pendling"}), \code{farg} och \code{antal_pendlare}
(\code{NA} för fokusområdets rutor).
}
\description{
Slår ihop fokusområdets rutor (svartmarkerade) med antingen in- eller
utpendlingsrutorna från \code{\link[=pendling_ruta]{pendling_ruta()}}s resultat till ett enda
\code{sf}-objekt, klart att kartlägga eller skriva till en GeoPackage-fil.
}
\seealso{
\code{\link[=pendling_ruta]{pendling_ruta()}}
}
