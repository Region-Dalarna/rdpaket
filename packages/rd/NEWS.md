# rd 0.0.0.9000

Första versionen. Paraplypaket: `library(rd)` attach:ar
`rdverktyg`, `rddiagram`, `rdgis`, `rdpostgres` och `rdshinyappar`.

`rdgeorouting` läggs till i `Depends` när det paketet är klart.
`rddeploy` och `rdadminportal` ingår inte - de laddas explicit.
