# rd 0.0.0.9001

Versionsbump utan innehållsändring (för att `remotes`/`pak`
tillförlitligt ska upptäcka uppdateringar i det här monorepot).

---

# rd 0.0.0.9000

Första versionen. Paraplypaket: `library(rd)` attach:ar
`rdverktyg`, `rddiagram`, `rdgis`, `rdpostgres`, `rdgeorouting` och
`rdshinyappar`.

`rddeploy` och `rdadminportal` ingår inte - de laddas explicit.
